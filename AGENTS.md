## Python

- Use test-driven development (red–green–refactor) best practices whenever possible.
- Run `black .` at the monorepo root before committing.

Git commits: Always include: [PIN-xxx] + one-line behavior change + evidence files + "why it matters" intent.